<nav class="navbar navbar-expand-lg navbar-dark bg-dark white-shadow-bottom">
    <a class="navbar-brand" href="/">Orpheos</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
        aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <!-- <li class="nav-item">
                <a class="nav-link" href="/profile">Profile</a>
            </li> -->
            <% if (typeof(navItems) === 'object' && navItems) { %>

                <% for (var i = 0; i < navItems.length; i++) { var navItem = navItems[i]; %>
                <li class="nav-item">
                    <a class="nav-link" href="<%= navItem.link %>"><%= navItem.name %></a>
                </li>
                <% } %>
            <% } %>
        </ul>
        <ul class="navbar-nav">
            <li class="nav-item">
                <a class="nav-link" href="/logout">
                    Logout <i class="fa fa-sign-out" aria-hidden="true"></i>
                </a>
            </li>
        </ul>
    </div>
</nav>